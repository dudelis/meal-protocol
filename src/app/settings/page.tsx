import React from 'react';
import Link from 'next/link'; // Assuming you're using React Router for navigation
import {
    Command,
    CommandDialog,
    CommandEmpty,
    CommandGroup,
    CommandInput,
    CommandItem,
    CommandList,
    CommandSeparator,
    CommandShortcut,
} from "@/components/ui/command"

const SettingsPage = () => {
    return (
        <div className="flex flex-col items-center justify-center q-full text-2xl">
            
        </div>
    );
};

export default SettingsPage;